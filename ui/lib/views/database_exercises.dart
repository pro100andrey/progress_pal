import 'package:flutter/material.dart';

class DatabaseExercises extends StatelessWidget {
  const DatabaseExercises({
    super.key,
  });

  @override
  Widget build(BuildContext context) => ListView(
    padding: const EdgeInsets.all(16),
    shrinkWrap: true,
  );
}
