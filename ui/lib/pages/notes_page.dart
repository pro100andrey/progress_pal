import 'package:flutter/material.dart';

class NotesPage extends StatelessWidget {
  const NotesPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    body: Stack(
      children: [
        Center(
          child: Text('NotesPage Coming soon...'),
        ),
      ],
    ),
  );
}
