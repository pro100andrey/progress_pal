import 'package:flutter/material.dart';

class BodyStatsPage extends StatelessWidget {
  const BodyStatsPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
    body: Stack(
      children: [
        Center(
          child: Text('BodyStatsPage Coming soon...'),
        ),
      ],
    ),
  );
}
